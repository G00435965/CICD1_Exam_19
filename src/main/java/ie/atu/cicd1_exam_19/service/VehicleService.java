package ie.atu.cicd1_exam_19.service;


import org.springframework.stereotype.Service;

@Service
public class VehicleService {
    private final List<vehicle> store = new ArrayList<> {
        for (vehicle v : store) {
            if (v.getregNumber().equals(id))
        }
    }
}
